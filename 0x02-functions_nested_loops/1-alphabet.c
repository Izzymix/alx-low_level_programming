#include "main.h"

/**
 * print alphabet - prints alphabet in lower case
 * Return: void
 */
void print_alphabet(void)
{
	char la;

	for (la = 'a'; la <= 'z'; la++)
		_putchar(la);

	_putchar('\n');
}
